### ishak bridge

ishak bridge is mcp adapter that uses [openai/tunnel-client](https://github.com/openai/tunnel-client) to allow chatgpt webui connect to your local pc and use codex toolchain

codex provides filesystem, code executer, network access

codex does not runs llm and does not consuming tokens
